# Prompt templates for automation

This folder is for prompt templates and instructions used when posting comments to Copilot (e.g. verify test results, closure request, checklist request).

Add templates here as needed; the main script can load them when building comment bodies.
